#include <stdlib.h>
#include <stdio.h>

typedef tno no;

struct tno{
    int info;
    //demais campos
    struct tno *prox, *ant; // Para listas duplamente ligadas
};

int main(void)
{
    no no1;

    
    return 0;
}

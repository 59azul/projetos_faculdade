#include <stdio.h>
#include <stdlib.h>

/*  RA do aluno tem que ser menor do que 1 000 000
    Nome do aluno tem que ser menor do que 50 caracteres
    Nota do aluno em formato ponto flutuante
    Programa deve: Inserir o cadastro de um aluno, remover o cadastro de um aluno,
    buscar aluno por RA, buscar aluno por nome e exibir dados de todos os alunos cadastrados.
    O programa deve utilizar um vetor de registros(structs) para armazenar os dados dos alunos.
    Nao devem ser utilizadas variaveis globais, todas opcoes como funcoes.
    
    Cada integrante do grupo deve postar em seu escaninho no AVA, em uma pasta
    chamada Atividade2, o código fonte produzido, identificado com o nome e o RA dos
    integrantes do grupo no início do arquivo.
    Também deve ser postado no AVA um documento em formato PDF,
    identificado com o nome e o RA dos integrantes do grupo no início do arquivo,
    contendo a descrição dos testes realizados e, para cada teste, um screenshot da tela
    (imagem em formato JPG) durante a execução do programa, demonstrando o resultado
    do teste e o correto funcionamento do programa. */



int main(void)
{
    
    
    
    
    return 0;
}